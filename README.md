# centcom-logistics-api
### Requires SQL Server to be running and Python 3.9+
To download all dependencies and set up the environment:
```
pip install -r requirements.txt
```
To start backend:
```
uvicorn main:app
```
